# catalog-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build catalog-contract` to build the library.

## Running unit tests

Run `nx test catalog-contract` to execute the unit tests via [Jest](https://jestjs.io).
