# inventory-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build inventory-contract` to build the library.

## Running unit tests

Run `nx test inventory-contract` to execute the unit tests via [Jest](https://jestjs.io).
